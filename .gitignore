# --- IGNORE LARGE FILES (GitHub Limit > 100MB) ---
ai-engine/ffmpeg.exe
ai-engine/ffprobe.exe
ai-engine/ffplay.exe

# --- IGNORE GARBAGE ---
.git
.DS_Store
Thumbs.db
__pycache__/
*.py[cod]

# --- ALLOW EVERYTHING ELSE (Team Setup) ---
# (Intentionally NOT ignoring node_modules, venv, uploads, etc.)